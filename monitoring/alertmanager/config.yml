global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'email-notifications'

receivers:
- name: 'email-notifications'
  email_configs:
  - to: 'myavarih@gmail.com'
    from: 'myavarih@gmail.com'
    smarthost: 'smtp.example.com:587'
    auth_username: 'myavarih@gmail.com'
    auth_password: ${SMTP_PASSWORD}

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']