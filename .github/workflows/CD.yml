# name: CD

# on:
#   push:
#     branches:
#       - feat/cd
#   pull_request:
#     branches:
#       - develop
#     types:
#       - closed

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Write .env file
#         run: |
#           echo "${{ secrets.ENV_FILE }}" > .env

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Login to GitHub Container Registry
#         uses: docker/login-action@v2
#         with:
#           registry: ghcr.io
#           username: ${{ secrets.USERNAME }}
#           password: ${{ secrets.PAT }}

#       - name: Build and Push Docker Image
#         run: |
#           IMAGE=ghcr.io/${{ secrets.USERNAME }}/backend:feat-cd
#           docker compose build
#           docker compose push

#       - name: Copy docker-compose.yml
#         uses: appleboy/scp-action@v1
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           password: ${{ secrets.SERVER_PASSWORD }}
#           source: "docker-compose.yml"
#           target: "/petYar/backend"

#       - name: Deploy on Server
#         uses: appleboy/ssh-action@v0.1.8
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           password: ${{ secrets.SERVER_PASSWORD }}
#           script: |
#             cd /petYar/backend
#             echo "${{ secrets.ENV_FILE }}" > .env
#             chmod 644 .env

#             docker compose down -v
#             docker image prune -f
#             sleep 5
#             docker pull ghcr.io/${{ secrets.USERNAME }}/backend:feat-cd
#             docker tag ghcr.io/${USERNAME}/backend:feat-cd backend_app_1:latest
#             docker compose up -d

name: CD

on:
  push:
    branches:
      - feat/cd
  pull_request:
    branches:
      - develop
    types:
      - closed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Write .env file
        run: |
          echo "${{ secrets.ENV_FILE }}" > .env

      # - name: Write .pem files
      #   run: |
      #     echo "${{ secrets.PRIVATE }}" > ./internal/infrastructure/jwt/private_key.pem
      #     echo "${{ secrets.PUBLIC }}" > ./internal/infrastructure/jwt/public_key.pem

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PAT }}

      - name: Build and Push Docker Image
        run: |
          docker compose build
          docker compose push

      - name: Copy configuration files
        uses: appleboy/scp-action@v0.1.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "docker-compose.yml"
          target: "/root/backend"

      - name: Deploy
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd /root/backend
            echo "${{ secrets.ENV_FILE }}" > .env
            chmod 644 .env

            docker compose down
            sleep 10
            docker compose pull
            docker compose up -d

# wow
